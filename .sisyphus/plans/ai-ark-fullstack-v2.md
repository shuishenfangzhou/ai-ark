# AI方舟 - 完整功能升级计划

## 🎯 目标

将静态网站升级为完整的动态 Web 应用，具备用户系统、AI 推荐、收藏管理等功能。

## 📊 现状分析

| 组件 | 当前状态 | 目标状态 |
|------|----------|----------|
| 前端 | 纯静态 HTML | 动态交互 + API 调用 |
| 后端 | FastAPI 骨架 | 完整 REST API |
| 数据库 | MySQL 容器 | 完整数据模型 |
| 认证 | 无 | JWT 用户系统 |
| AI | 无 | Gemini API 集成 |
| 部署 | Docker Compose | Docker Compose (优化) |

## 🗂️ 数据库架构

### 用户模块
```
users
├── id (PK)
├── username (唯一)
├── email (唯一)
├── password_hash
├── avatar
├── created_at
└── updated_at
```

### 工具模块
```
tools
├── id (PK)
├── name
├── description
├── url
├── logo
├── category (外键)
├── pricing (Free/Paid)
├── rating
├── visits
├── source
├── created_at
└── updated_at
```

### 分类模块
```
categories
├── id (PK)
├── name
├── icon
├── description
└── sort_order
```

### 收藏模块
```
favorites
├── id (PK)
├── user_id (外键)
├── tool_id (外键)
├── note (用户备注)
├── created_at
└── UNIQUE(user_id, tool_id)
```

### 搜索历史
```
search_history
├── id (PK)
├── user_id (外键，可为空)
├── query
├── results_count
├── created_at
```

## 📡 API 设计

### 认证接口
```
POST   /api/auth/register     # 注册
POST   /api/auth/login        # 登录
POST   /api/auth/logout       # 登出
GET    /api/auth/me           # 获取当前用户
PUT    /api/auth/password     # 修改密码
```

### 工具接口
```
GET    /api/tools             # 工具列表（分页、筛选）
GET    /api/tools/:id         # 工具详情
GET    /api/tools/search?q=   # 搜索工具
GET    /api/categories        # 分类列表
GET    /api/categories/:id    # 分类详情
```

### 收藏接口
```
GET    /api/favorites         # 我的收藏
POST   /api/favorites         # 添加收藏
DELETE /api/favorites/:id     # 取消收藏
```

### AI 推荐接口
```
POST   /api/recommend         # AI 工具推荐
POST   /api/chat              # AI 对话（Gemini）
```

### 用户接口
```
GET    /api/users/profile     # 我的资料
PUT    /api/users/profile     # 更新资料
GET    /api/users/stats       # 使用统计
```

## 🎨 前端功能

### 页面结构
```
src/frontend/
├── index.html           # 首页（工具展示）
├── login.html           # 登录/注册
├── profile.html         # 个人中心
├── tool.html            # 工具详情
├── search.html          # 搜索结果
├── favorites.html       # 我的收藏
└── admin.html           # 管理后台（可选）
```

### 交互功能
- ✅ 用户注册/登录（JWT 认证）
- ✅ 工具搜索（实时搜索 + 分页）
- ✅ 收藏/取消收藏
- ✅ 个性化推荐（AI Gemini）
- ✅ 历史记录
- ✅ 响应式 UI

## 🚀 部署架构

```
Internet
    │
    ▼
┌─────────────┐
│   Nginx     │ :80/:443
│  (1Panel)   │
└──────┬──────┘
       │
       ▼
┌─────────────┐     ┌─────────────┐
│  Frontend   │     │   Backend   │
│   (静态)    │◄───►│  FastAPI    │
└──────┬──────┘     │   :8000     │
       │            └──────┬──────┘
       ▼                   │
┌─────────────┐     ┌──────▼──────┐
│   静态资源   │     │   MySQL     │
│  (dist/)    │     │   :3306     │
└─────────────┘     └─────────────┘
```

## 📋 实施步骤

1. **数据库设计** ✅ 本阶段
2. **后端 API 开发** ⏳ 下阶段
3. **前端动态化** ⏳ 下阶段
4. **AI 集成** ⏳ 下阶段
5. **测试与部署** ⏳ 最后

## 📅 时间预估

| 阶段 | 内容 | 时间 |
|------|------|------|
| Phase 1 | 数据库 + 基础 API | 2-3 小时 |
| Phase 2 | 认证 + 工具 API | 2-3 小时 |
| Phase 3 | 前端交互 | 3-4 小时 |
| Phase 4 | AI 集成 | 1-2 小时 |
| Phase 5 | 测试 + 部署 | 1 小时 |

**总计: 10-13 小时**

## 🔑 依赖

- Python 3.9+
- MySQL 8.0
- FastAPI
- JWT Authentication
- Google Gemini API Key

## 📝 备注

- 前端保持轻量，尽量用原生 JS
- 后端遵循 RESTful 规范
- 数据库使用外键约束
- 密码加密存储（bcrypt）
- Token 使用 JWT
