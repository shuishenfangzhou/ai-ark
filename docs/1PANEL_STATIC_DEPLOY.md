# AI方舟 - 1Panel 静态网站部署指南 (推荐)

由于本项目已重构为**纯静态前端应用** (Mock Data 版本)，不再依赖后端 API 服务。
因此，最简单、最高效的部署方式是使用 1Panel 的 **"静态网站"** 功能。

## 📋 部署步骤

### 步骤 1: 准备构建产物

1. 在本地项目根目录运行构建命令 (已完成):
   ```bash
   npm run build
   ```
   这会生成 `dist` 目录，里面包含了所有静态文件 (`index.html`, `assets/` 等)。

2. 将 `dist` 目录打包为压缩包 (例如 `dist.zip` 或 `dist.tar.gz`)。

### 步骤 2: 上传到服务器

1. 登录 1Panel 面板。
2. 进入 **「文件」** 管理。
3. 进入 `/opt/1panel/apps/openresty/www/` 目录 (或者你存放网站文件的任意目录)。
4. 创建一个新文件夹，例如 `ai-ark`。
5. 进入 `ai-ark` 文件夹，上传并解压刚才的 `dist.zip`。
   - 确保解压后，该目录下直接包含 `index.html` (如果解压出了 `dist` 子文件夹，请把内容移动到 `ai-ark` 根目录)。
   - 最终路径结构应类似: `/opt/1panel/apps/openresty/www/ai-ark/index.html`

### 步骤 3: 创建静态网站

1. 进入 **「网站」** -> **「网站」**。
2. 点击 **「创建网站」**。
3. 选择 **「静态网站」** (或者 "运行环境" 选择 "静态网站" / "Nginx")。
4. 填写配置:
   - **主域名**: 填写你的域名 (例如 `ai.example.com`) 或服务器 IP。
   - **网站目录**: 选择刚才上传的目录 `/opt/1panel/apps/openresty/www/ai-ark`。
5. 点击 **「确认」**。

### 步骤 4: 访问验证

1. 在浏览器中访问你设置的域名或 IP。
2. 你应该能看到 "AI方舟" 的首页，顶部有蓝色的统计卡片 (1428 收录)，且不再显示 "0 items"。

---

## 🐳 备选方案: Docker 部署

如果你更喜欢使用 Docker 部署，可以使用以下简化版的 `docker-compose.static.yml`。

1. 在服务器上创建 `docker-compose.yml`:
   ```yaml
   version: '3'
   services:
     ai-ark-static:
       image: nginx:alpine
       container_name: ai-ark-static
       ports:
         - "8080:80"  # 宿主机端口:容器端口
       volumes:
         - ./dist:/usr/share/nginx/html:ro  # 挂载 dist 目录
         - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro # 挂载 nginx 配置
       restart: always
   ```

2. 上传 `dist` 目录和 `docker/nginx.conf` 到同级目录。
3. 运行 `docker-compose up -d`。
4. 访问 `http://服务器IP:8080`。

