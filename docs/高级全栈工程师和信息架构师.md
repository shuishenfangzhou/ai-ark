作为高级全栈工程师和信息架构师，请帮我将当前的 AI 工具站升级为类似 `ai-bot.cn` 的“门户级”架构。

**核心任务：**

1. **实现 SPA 详情页切换逻辑**：
   - 修改 `ai_tools_dashboard.html`，不再使用 `alert` 提示。实现一个“视图切换”系统：
     - **列表视图**：当前的工具方格矩阵。
     - **详情视图**：当用户点击工具图标或名字时，隐藏列表，显示该工具的专属详情页。
   - **详情页内容要求**：包含大 Logo、工具标题、详细长篇介绍（200字以上）、核心功能点（列表形式）、官方链接按钮、以及“相关工具推荐”栏目。
   - 实现“返回首页”的导航功能。
2. **升级数据采集器 (`tools_manager.py`) - 学习 ai-bot.cn**：
   - 升级 Python 脚本，使其具备“批量数据扩展”能力。
   - **自动化描述生成**：在脚本中引入逻辑，如果工具没有描述，利用本地 LLM 接口或预设模板，根据工具名和类别自动生成一段伪真实的深度介绍，确保每个详情页内容充实。
   - **数据规模化**：将 `tools_db` 扩展到支持从外部 JSON 或 CSV 加载。请先帮我预置 100 个以上的核心工具数据，覆盖 ai-bot.cn 的所有二级分类（如：AI 绘画、AI 视频、AI 编程、AI 提示词、AI 学习等）。
3. **视觉与体验复刻**：
   - **面包屑导航**：在详情页顶部增加 `首页 > AI写作 > ChatGPT` 这样的导航。
   - **SEO 优化结构**：虽然是单页应用，但请在 JS 中动态修改 `document.title`，当进入详情页时显示“工具名 - AI 工具导航”。
   - **右侧侧边栏**：详情页增加右侧固定栏，显示“热门工具榜单”。

**执行步骤：**

1. 先重写 HTML 中的 `render` 函数，支持视图状态切换（`currentView: 'home' | 'detail'`）。
2. 更新 `tools_manager.py`，增加长文本描述字段 `long_desc` 和功能列表字段 `features`。
3. 运行脚本，生成包含 100+ 工具的 `js/tools_data.js`。
4. 告诉我如何继续扩展到 1000+ 工具。



1. **分批次收录**：不要一次性写 1000 个。让 Trae 帮你写一个 `scraper_helper.py`。这个脚本可以爬取一些公开的 AI 工具目录网站（如 *Futurepedia* 或 *AlternativeTo*）的 API 或页面。
2. **数据本地化**：由于 1000 张 Logo 图片很大，你可以要求 Trae 在脚本中加入**图片压缩**功能（使用 Python 的 `Pillow` 库），将每张 Logo 限制在 10KB 以内，这样你的网页加载 1000 个工具也不会卡顿。
3. **分页或无限滚动**：当工具超过 100 个时，一次性显示会很慢。你可以让 Trae 在提示词中加入：“**请实现滚动加载（Lazy Load），每次只加载 20 个工具卡片**”。