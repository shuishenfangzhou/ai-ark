---
// src/pages/index.astro
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Sidebar from '../components/Sidebar.astro';
import ToolList from '../components/ToolList.tsx';
import Footer from '../components/Footer.astro';
import CompareModal from '../components/CompareModal.astro';
import FavoritesModal from '../components/FavoritesModal.astro';
import HotSidebar from '../components/HotSidebar.astro';
import SEO from '../components/SEO.astro';
import WechatModal from '../components/WechatModal.astro';

import toolsData from '../../data/tools.json';
const tools = toolsData.tools;
---

<Layout>
    <SEO 
        title="AI方舟 | 216+ AI工具导航"
        description="AI方舟(AIArk)收录216个AI工具，提供最全面的AI工具导航服务。包含AI写作、图像、视频、办公、编程等全品类AI应用。"
        tools={[]}
    />
    
    <Header />
    
    <div class="w-full px-2 sm:px-4 flex flex-grow items-start gap-4">
        <Sidebar />
        
        <main class="flex-1 min-w-0 px-2 sm:px-4 py-6">
            <!-- Stats Dashboard -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                <div class="bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl p-6 text-white shadow-lg">
                    <div class="flex items-center gap-3 mb-2">
                        <div class="w-10 h-10 bg-white/20 rounded-lg flex items-center">
                            <i class="fa-solid fa-database"></i>
                        </div>
                        <span class="text-blue-100 font-medium">已收录工具</span>
                    </div>
                    <div class="text-4xl font-bold mb-2">{tools.length}</div>
                    <div class="flex items-center text-sm text-blue-100">
                        <span class="bg-white/20 px-2 py-0.5 rounded text-xs mr-2">实时更新</span>
                        <span>覆盖 {new Set(tools.map((t: any) => t.category)).size} 个核心领域</span>
                    </div>
                </div>
                
                <div class="bg-white rounded-2xl p-4 border border-slate-100 shadow-sm">
                    <h3 class="font-bold text-slate-800 mb-4">收录分布</h3>
                    <div class="space-y-3 text-sm">
                        <div class="flex items-center justify-between">
                            <span class="flex items-center"><span class="w-2 h-2 rounded-full bg-blue-500 mr-2"></span>AI 对话</span>
                            <span class="font-medium">{tools.filter((t: any) => t.category === 'AI对话').length}</span>
                        </div>
                        <div class="flex items-center justify-between">
                            <span class="flex items-center"><span class="w-2 h-2 rounded-full bg-purple-500 mr-2"></span>AI 绘画</span>
                            <span class="font-medium">{tools.filter((t: any) => t.category === 'AI绘画').length}</span>
                        </div>
                        <div class="flex items-center justify-between">
                            <span class="flex items-center"><span class="w-2 h-2 rounded-full bg-amber-500 mr-2"></span>AI 写作</span>
                            <span class="font-medium">{tools.filter((t: any) => t.category === 'AI写作').length}</span>
                        </div>
                        <div class="flex items-center justify-between">
                            <span class="flex items-center"><span class="w-2 h-2 rounded-full bg-pink-500 mr-2"></span>AI 视频</span>
                            <span class="font-medium">{tools.filter((t: any) => t.category === 'AI视频').length}</span>
                        </div>
                        <div class="flex items-center justify-between">
                            <span class="flex items-center"><span class="w-2 h-2 rounded-full bg-emerald-500 mr-2"></span>AI 编程</span>
                            <span class="font-medium">{tools.filter((t: any) => t.category === 'AI编程').length}</span>
                        </div>
                    </div>
                </div>
                
                <div class="bg-white rounded-2xl p-6 border border-slate-100 shadow-sm">
                    <div class="flex items-center justify-between mb-4 pb-4 border-b border-slate-50">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 bg-green-50 text-green-600 rounded-lg flex items-center justify-center">
                                <i class="fa-solid fa-calendar-plus"></i>
                            </div>
                            <div>
                                <div class="text-xs text-slate-400">今日新增</div>
                                <div class="text-xl font-bold text-slate-800">+{Math.floor(Math.random() * 20) + 5}</div>
                            </div>
                        </div>
                        <span class="text-xs text-green-500 bg-green-50 px-2 py-1 rounded-full">4.2%</span>
                    </div>
                    <div class="flex items-center justify-between">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 bg-purple-50 text-purple-600 rounded-lg flex items-center justify-center">
                                <i class="fa-solid fa-chart-simple"></i>
                            </div>
                            <div>
                                <div class="text-xs text-slate-400">总访问量</div>
                                <div class="text-xl font-bold text-slate-800">{(Math.random() * 2 + 0.8).toFixed(1)}M</div>
                            </div>
                        </div>
                        <span class="text-xs text-purple-500 bg-purple-50 px-2 py-1 rounded-full">Top 1%</span>
                    </div>
                </div>
            </div>

            <h1 class="text-2xl font-bold text-slate-900 mb-6">全部工具</h1>
            
            <ToolList client:load />
        </main>
        
        <HotSidebar />
    </div>
    
    <CompareModal />
    <FavoritesModal />
    <WechatModal />
    
    <dialog id="tool-detail-modal" class="p-0 rounded-2xl backdrop:bg-black/60 shadow-2xl w-[800px] max-w-[95vw] max-h-[90vh] overflow-hidden border-0">
      <div class="bg-white">
        <!-- Header with gradient matching ai-bot.cn style -->
        <div class="bg-gradient-to-r from-blue-600 to-indigo-600 p-6 text-white">
          <div class="flex items-start gap-5">
            <div id="modal-icon" class="w-20 h-20 rounded-full bg-white/20 backdrop-blur flex items-center justify-center text-4xl shadow-lg flex-shrink-0">
              🤖
            </div>
            <div class="flex-1">
              <div class="flex justify-between items-start">
                <div>
                  <h2 id="modal-title" class="text-2xl font-bold mb-3">工具名称</h2>
                  <div class="flex items-center gap-3 text-sm text-blue-100 flex-wrap">
                    <span id="modal-category" class="bg-white/20 px-3 py-1 rounded-full">AI对话</span>
                    <span id="modal-rating" class="flex items-center gap-1 bg-white/10 px-2 py-1 rounded">
                      <i class="fa-solid fa-star text-amber-400"></i>
                      <span class="font-medium text-white">0.0</span>
                    </span>
                    <span id="modal-pricing" class="bg-green-500/80 px-2 py-0.5 rounded text-xs">免费</span>
                  </div>
                </div>
                <button onclick="document.getElementById('tool-detail-modal').close()" class="text-white/80 hover:text-white hover:bg-white/20 rounded-lg p-2 transition">
                  <i class="fa-solid fa-xmark text-xl"></i>
                </button>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Content -->
        <div class="p-6 max-h-[50vh] overflow-y-auto">
          <!-- Description -->
          <div class="mb-6">
            <h3 class="font-semibold text-slate-800 mb-3 flex items-center gap-2">
              <i class="fa-solid fa-circle-info text-blue-500"></i>
              工具介绍
            </h3>
            <p id="modal-desc" class="text-slate-600 leading-relaxed bg-slate-50 p-4 rounded-xl border border-slate-100">
              工具描述...
            </p>
          </div>
          
          <!-- Features -->
          <div class="mb-6">
            <h3 class="font-semibold text-slate-800 mb-3 flex items-center gap-2">
              <i class="fa-solid fa-star text-amber-500"></i>
              核心功能
            </h3>
            <div id="modal-features" class="grid grid-cols-2 gap-3">
              <!-- Features will be inserted here -->
            </div>
          </div>
          
          <!-- Tags -->
          <div class="mb-4">
            <h3 class="font-semibold text-slate-800 mb-3 flex items-center gap-2">
              <i class="fa-solid fa-tags text-purple-500"></i>
              相关标签
            </h3>
            <div id="modal-tags" class="flex flex-wrap gap-2">
              <!-- Tags will be inserted here -->
            </div>
          </div>
        </div>
        
        <!-- Footer Actions matching ai-bot.cn style -->
        <div class="border-t border-slate-100 p-4 bg-slate-50">
          <div class="flex justify-between items-center">
            <button class="flex items-center gap-2 px-4 py-2 text-slate-600 hover:bg-slate-200 rounded-lg transition">
              <i class="fa-regular fa-heart text-red-500"></i>
              收藏
            </button>
            <div class="flex gap-3">
              <button class="px-4 py-2 text-slate-600 hover:bg-slate-200 rounded-lg transition flex items-center gap-2">
                <i class="fa-solid fa-code-compare"></i>
                对比
              </button>
              <a id="modal-link" href="#" target="_blank" class="px-6 py-2 bg-gradient-to-r from-blue-600 to-indigo-600 text-white rounded-lg hover:from-blue-700 hover:to-indigo-700 shadow-lg shadow-blue-500/25 flex items-center gap-2 transition font-medium">
                前往官网 <i class="fa-solid fa-arrow-right ml-1"></i>
              </a>
            </div>
          </div>
        </div>
      </div>
    </dialog>
    
    <script is:inline>
      window.openToolDetail = (tool) => {
        const modal = document.getElementById('tool-detail-modal');
        
        // Update basic info
        document.getElementById('modal-title').innerText = tool.name || '未知工具';
        document.getElementById('modal-desc').innerText = tool.description_full || tool.description || '暂无描述';
        document.getElementById('modal-link').href = tool.url || '#';
        document.getElementById('modal-category').innerText = tool.category || 'AI工具';
        document.getElementById('modal-rating').innerHTML = `
          <i class="fa-solid fa-star text-amber-400"></i>
          <span class="font-medium text-white">${(tool.rating || 0).toFixed(1)}</span>
        `;
        
        // Pricing badge
        const pricingText = {
          'free': '免费',
          'freemium': '免费增值',
          'paid': '付费',
          'contact': '联系定价'
        };
        document.getElementById('modal-pricing').innerText = pricingText[tool.pricing] || tool.pricing || '免费';
        
        // Features - match ai-bot.cn style
        const featuresContainer = document.getElementById('modal-features');
        const features = tool.features || [];
        if (features.length > 0) {
          featuresContainer.innerHTML = features.map(f => `
            <div class="flex items-center gap-2 bg-blue-50 text-blue-700 px-4 py-2.5 rounded-lg text-sm">
              <i class="fa-solid fa-check text-blue-500"></i>
              ${f}
            </div>
          `).join('');
        } else {
          featuresContainer.innerHTML = '<div class="col-span-2 text-slate-400 text-sm">暂无功能信息</div>';
        }
        
        // Tags - match ai-bot.cn style
        const tagsContainer = document.getElementById('modal-tags');
        const tags = tool.tags || [];
        if (tags.length > 0) {
          tagsContainer.innerHTML = tags.map(tag => `
            <span class="px-3 py-1.5 bg-slate-100 text-slate-600 rounded-full text-sm hover:bg-slate-200 cursor-pointer transition border border-slate-200">${tag}</span>
          `).join('');
        } else {
          tagsContainer.innerHTML = '<span class="text-slate-400 text-sm">暂无标签</span>';
        }
        
        modal.showModal();
      }
    </script>
    
    <Footer />
</Layout>
